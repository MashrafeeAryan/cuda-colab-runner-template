# cuda-colab-runner-template
A simple Google Colab template for running CUDA/C++ code on a GPU without needing a local NVIDIA GPU. It checks the GPU, clones a GitHub repo, compiles CUDA code with nvcc, and runs the executable.
